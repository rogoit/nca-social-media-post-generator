{
  "name": "social-media-post-generator",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest test/unit test/functional",
    "test:run": "vitest run test/unit test/functional",
    "test:unit": "vitest run test/unit",
    "test:functional": "vitest run test/functional",
    "test:real": "vitest run test/real",
    "test:all": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:coverage:html": "vitest run --coverage --reporter=html",
    "test:coverage:full": "vitest run --coverage --coverage.reporter=html --coverage.reporter=text --coverage.reportsDirectory=coverage",
    "test:ui": "vitest --ui",
    "type-check": "tsc --noEmit --noUnusedLocals --allowImportingTsExtensions --skipLibCheck",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.41.0",
    "@astrojs/tailwind": "^6.0.2",
    "@google/generative-ai": "^0.1.3",
    "astro": "^5.12.3",
    "tailwindcss": "^3.0.24"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "jsdom": "^26.1.0",
    "prettier": "^3.6.2",
    "prettier-plugin-astro": "^0.14.1",
    "typescript": "^5.3.3",
    "vitest": "^3.2.4"
  }
}
